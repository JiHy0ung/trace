name: CI
run-name: Trace GitHub Actions

on: [push]

jobs:
  ci:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v6
      - name: Use Node.js
        uses: actions/node@v4
        with:
          node-version: 20

      - run: npm ci
      - run: npm lint
      - run: npm typecheck
